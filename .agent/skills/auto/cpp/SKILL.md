---
name: cpp
description: "Cpp for Fullsword. 1 conventions, 1 fixes."
domain: cpp
triggers:
  - glob: "**/*.cpp"
  - glob: "**/*.h"
autoGenerated: true
enabled: true
---

# Cpp

Auto-compiled from **6 real patterns** in **Fullsword**. This skill is auto-routed to agents when working on cpp files.

## 🔧 Problem Playbooks

### Fixed null crash in Track
-     if (!povActive) return;
+     static uint32_t lastPovEndMs = 0;
- 
+     static bool     wasPovActive = false;
-     // Intensity: 0 at 55 deg/s, 1.0 at 250 deg/s.  Clamp both ends.
+     static uint8_t  currentRainbowHue = 0;
-     float intensity = constrain((povPeak - ENTER_SPEED) / 200.0f, 0.0f, 1.0f);
+ 
-     if (intensity < 0.05f) return;
+     // Track state transitions for per-swing

**Actionable Steps:**
1. Modified 1 files
2. identifier: Track
3. identifier: Shift
4. identifier: Post
5. identifier: Strike

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **decision in EffectPOV.cpp — confirmed 3x** — -         for (int i = HILT_LEDS; i < BLADE_LENGTH; i++) {
+         float activeTime = (float)(mill

## 🤔 Decisions & Trade-offs

- **decision in EffectPOV.cpp** — - 
+     static uint32_t strikeStartMs = 0;
-     // Track state transitions for per-swing effects
+
- **decision in EffectPOV.cpp** — -     // MODE 0 — FIRE POV
+     // MODE 0 — FIRE POV (Slash Trails)
-     // What you see in the ai

---
*Auto-generated by BrainSync 🧠 | 6 patterns | 2026-03-29*
