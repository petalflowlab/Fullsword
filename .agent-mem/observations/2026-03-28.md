# Notes — 2026-03-28
> 6 notes

---

## Git Commit: lightning mode correctionand hikt behavor
Source: auto

Captured via Git commit.
Message: lightning mode correctionand hikt behavor
Branch: simple

---

## Updated schema Hilt — ensures atomic multi-step database operations
Source: file-change

-   if (gyroZ_abs > 30.0f) {
+   static float dominantFade = 0.0f;
-     // ── Hilt Strobe (Special Area: bladePos 0..HILT_LEDS-1) ────────────
+   static uint8_t dominantHue = 0;
-     if (gyroZ_abs > 100.0f) {
+   const float dt = 0.016f;
-       bool strobeOn = (millis() % 40 < 20); // 25Hz strobe
+ 
-       for (int i = 0; i < HILT_LEDS; i++) {
+   if (gyroZ_abs > 30.0f) {
-         if (strobeOn) {
+     if (dominantFade <= 0.0f) dominantHue = (uint8_t)(millis() * 0.1f);
-           CRGB col… [truncated]

**Facts:**
- Modified 1 files
- identifier: Hilt
- identifier: Strobe
- identifier: Special
- identifier: Area
- identifier: CRGB
- identifier: White
- identifier: Blue
- identifier: Sequence

**Files:** `Fullsword.ino`

---

## Updated schema CRGB — ensures atomic multi-step database operations
Source: file-change

-   static float dominantFade = 0.0f;
+   if (gyroZ_abs > 30.0f) {
-   static uint8_t dominantHue = 0;
+     rollPhase += (twistRate * 0.005f);
-   const float dt = 0.016f;
+     int numSparks = constrain((int)(gyroZ_abs / 20.0f), 1, 8);
- 
+     for (int i = 0; i < numSparks; i++) {
-   if (gyroZ_abs > 30.0f) {
+       int target = random(0, BLADE_LENGTH);
-     // ── Global spark overlay ──
+       float shift = sinf(rollPhase + (target * 0.1f)) + 1.0f;
-     rollPhase += (twistRate * 0.005f);… [truncated]

**Facts:**
- Modified 1 files
- identifier: CRGB
- identifier: CHSV
- identifier: ESP
- identifier: NOW
- identifier: SYNC
- identifier: SyncPacket
- identifier: MicPacket
- identifier: Serial

**Files:** `purplesword/purplesword.ino`

---

## Updated schema Global — ensures atomic multi-step database operations
Source: file-change

-   if (gyroZ_abs > 30.0f) {
+   static float dominantFade = 0.0f;
-     rollPhase += (twistRate * 0.005f);
+   static uint8_t dominantHue = 0;
-     int numSparks = constrain((int)(gyroZ_abs / 20.0f), 1, 8);
+   const float dt = 0.016f;
-     for (int i = 0; i < numSparks; i++) {
+ 
-       int target = random(0, BLADE_LENGTH);
+   if (gyroZ_abs > 30.0f) {
-       float shift = sinf(rollPhase + (target * 0.1f)) + 1.0f;
+     // ── Global spark overlay ──
-       CRGB sparkColor = CHSV(baseHue +… [truncated]

**Facts:**
- Modified 1 files
- identifier: Global
- identifier: CRGB
- identifier: CHSV
- identifier: Capture
- identifier: Head
- identifier: Tail
- identifier: Roll
- identifier: Strobe

**Files:** `purplesword/purplesword.ino`

---

## discovery in Config.h
Source: file-change

- 
+ #define HILT_LEDS     0
- // ─── IMU ──────────────────────────────────────────────────────────────
+ #define BLADE_PIXELS  BLADE_LENGTH
- #define IMU_ADDR      0x68
+ 
- 
+ // ─── IMU ──────────────────────────────────────────────────────────────
- // ─── BUTTON / SYNC TIMING ─────────────────────────────────────────────
+ #define IMU_ADDR      0x68
- #define DCLICK_MS       350
+ 
- #define BOOST_HOLD_MS   500
+ // ─── BUTTON / SYNC TIMING ─────────────────────────────────────────────
- #… [truncated]

**Facts:**
- Modified 1 files

**Files:** `purplesword/Config.h`

---

## Updated schema Hilt — ensures atomic multi-step database operations
Source: file-change

-     rollPhase += (twistRate * 0.005f);
+     // ── Hilt Strobe (Special Area: bladePos 0..HILT_LEDS-1) ────────────
-     int numSparks = constrain((int)(gyroZ_abs / 20.0f), 1, 8);
+     if (gyroZ_abs > 100.0f) {
-     for (int i = 0; i < numSparks; i++) {
+       bool strobeOn = (millis() % 40 < 20); // 25Hz strobe
-       int target = random(0, BLADE_LENGTH);
+       for (int i = 0; i < HILT_LEDS; i++) {
-       float shift = sinf(rollPhase + (target * 0.1f)) + 1.0f;
+         if (strobeOn) … [truncated]

**Facts:**
- Modified 1 files
- identifier: Hilt
- identifier: Strobe
- identifier: Special
- identifier: Area
- identifier: CRGB
- identifier: White
- identifier: Blue
- identifier: Sequence

**Files:** `Fullsword.ino`

