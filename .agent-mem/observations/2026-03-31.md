# Notes — 2026-03-31
> 15 notes

---

## decision in EffectForestFairies.cpp
Source: file-change

-   bool swingPredict = (swingMag > 50.0f && dSwing > 15.0f);
+   bool swingPredict = (swingMag > 35.0f && dSwing > 10.0f); // Easier slash ignition
-   bool swingStrong  = (swingMag > 200.0f);
+   bool swingStrong  = (swingMag > 160.0f);                  // Easier pure swing override
-   bool thrustStrong = (fabsf(dAY) > 0.5f);
+   bool thrustStrong = (fabsf(dAY) > 0.4f);
-   if (swingMag > 140.0f && fabsf(dSwing) < 20.0f) {
+   if (swingMag > 180.0f && fabsf(dSwing) < 15.0f) {
-   } else if (s… [truncated]

**Facts:**
- Modified 1 files

**Files:** `EffectForestFairies.cpp`

---

## Updated spin database schema
Source: file-change

-   if ((swingPredict || swingStrong || thrustStrong) && !isAttacking) {
+   static bool isSpinning = false;
-     lastAttack = millis();
+   static uint32_t spinStartMs = 0;
-     // Blast ALL fairies up the blade instantly
+   
-     for (int i = 0; i < MAX_FAIRIES; i++) {
+   // A spin is a sustained high velocity without the aggressive, instantaneous snapping acceleration of a strike
-         fairies[i].active = true;
+   if (swingMag > 140.0f && fabsf(dSwing) < 20.0f) {
-         fairies[i… [truncated]

**Facts:**
- Modified 1 files
- identifier: Only
- identifier: NOT
- identifier: Blast
- identifier: ALL
- identifier: High
- identifier: Mix
- identifier: SUDDEN
- identifier: STOP

**Files:** `EffectForestFairies.cpp`

---

## tool-pattern in EffectRainbow.cpp
Source: file-change

- git
+ 

**Facts:**
- Modified 1 files

**Files:** `EffectRainbow.cpp`

---

## Git Commit: 4 modes
Source: auto

Captured via Git commit.
Message: 4 modes
Branch: simple

---

## what-changed in EffectRainbow.cpp — confirmed 3x
Source: file-change

- 
+ git

**Facts:**
- Modified 1 files

**Files:** `EffectRainbow.cpp`

---

## Fixed null crash in Overlapping
Source: file-change

-   for (int i = HILT_LEDS; i < BLADE_LENGTH; i++) {
+   static float bandPhase1 = 0.0f;
-     // Deep, glowing forest green baseline with slow shifting pulse
+   static float bandPhase2 = 0.0f;
-     float breath = 0.3f + 0.15f * sinf(globalSwayPhase * 0.3f + (float)i * 0.1f);
+   bandPhase1 += 0.015f + sway * 0.5f;
-     if (!isAttacking) {
+   bandPhase2 -= 0.008f + sway * 0.2f;
-       bladeSet(i, CHSV(96, 240, (uint8_t)(breath * 255.0f))); // 96 is strict green in FastLED
+ 
-     } else {
… [truncated]

**Facts:**
- Modified 1 files
- identifier: Overlapping
- identifier: Combine
- identifier: Animate
- identifier: CHSV
- identifier: During
- identifier: UPDATE
- identifier: RENDER
- identifier: FAIRIES

**Files:** `EffectForestFairies.cpp`

---

## Updated schema CRGB — ensures atomic multi-step database operations
Source: file-change

-             default: color = CRGB::White; break;
+             case MODE_FOREST:
-           }
+               color = (random8() < 40) ? CRGB(200, 255, 200) : CRGB(0, 200, 50);
-           bladeSet(i, color);
+               break;
-         } else {
+             default: color = CRGB::White; break;
-           bladeSet(i, CRGB::Black);
+           }
-         }
+           bladeSet(i, color);
-       }
+         } else {
-     }
+           bladeSet(i, CRGB::Black);
- 
+         }
-     // … [truncated]

**Facts:**
- Modified 1 files
- identifier: CRGB
- identifier: White
- identifier: Black
- identifier: Rest
- identifier: Original
- identifier: Spark
- identifier: Effect
- identifier: CHSV

**Files:** `Fullsword.ino`

---

## discovery in Config.h
Source: file-change

-   MODE_COUNT     = 3
+   MODE_FOREST    = 3,
- };
+   MODE_COUNT     = 4
- 
+ };
- // ─── GLOBALS (defined in Fullsword.ino) ───────────────────────────────
+ 
- extern CRGB     leds[NUM_LEDS];
+ // ─── GLOBALS (defined in Fullsword.ino) ───────────────────────────────
- extern bool     imuOk;
+ extern CRGB     leds[NUM_LEDS];
- extern float    swingMag, currSwing, twistRate;
+ extern bool     imuOk;
- extern float    accelX, accelY, accelZ;
+ extern float    swingMag, currSwing, twistRate;
- … [truncated]

**Facts:**
- Modified 1 files

**Files:** `Config.h`

---

## trade-off in EffectRainbow.cpp
Source: file-change

-   float dSwing = swingMag - prevSwingMag;
+   static uint8_t  swingHue = 0;
-   prevSwingMag = swingMag;
+   float dSwing = swingMag - prevSwingMag;
- 
+   prevSwingMag = swingMag;
-   // ── SUDDEN STOP DETECTION ─────────────────────────────────────────────
+ 
-   static uint32_t lastStopDetectMs = 0;
+   // ── SUDDEN STOP DETECTION ─────────────────────────────────────────────
-   if (swingMag > 50.0f && dSwing < -15.0f && (currSwing < 40.0f) && (millis() - lastStopDetectMs > 500)) {
+   sta… [truncated]

**Facts:**
- Modified 1 files

**Files:** `EffectRainbow.cpp`

---

## Fixed null crash in Travel — confirmed 3x
Source: file-change

-         lava[i] += wobbleAmp * sinf(lavaPhase * 1.8f + (float)i * 0.22f);
+         // Travel UP from hilt to tip! Notice the negative sign for i
-         lava[i] = constrain(lava[i], 0.0f, 1.0f);
+         lava[i] += wobbleAmp * sinf(lavaPhase * 2.8f - (float)i * 0.42f);
-     }
+         lava[i] = constrain(lava[i], 0.0f, 1.0f);
- 
+     }
-     float pulseR = 0.88f + 0.12f * sinf(shimmerPhase * 0.70f);
+ 
-     float pulseO = 0.86f + 0.14f * sinf(shimmerPhase * 1.20f + 1.0f);
+     float p… [truncated]

**Facts:**
- Modified 1 files
- identifier: Travel
- identifier: Notice
- identifier: Flicker
- identifier: CRGB
- identifier: PERMANENT
- identifier: HILT
- identifier: BASE
- identifier: FIRE

**Files:** `EffectFire.cpp`

---

## what-changed in EffectFire.cpp
Source: file-change

-         uint8_t bri = (uint8_t)(fireTrai  l[i] * 240.0f * sparkle);
+         uint8_t bri = (uint8_t)(fireTrail[i] * 240.0f * sparkle);

**Facts:**
- Modified 1 files

**Files:** `purplesword/EffectFire.cpp`

---

## what-changed in EffectFire.cpp
Source: file-change

-         uint8_t bri = (uint8_t)(fireTrail[i] * 240.0f * sparkle);
+         uint8_t bri = (uint8_t)(fireTrai  l[i] * 240.0f * sparkle);

**Facts:**
- Modified 1 files

**Files:** `purplesword/EffectFire.cpp`

---

## Fixed null crash in EFFECT
Source: file-change

- // EFFECT: LIGHTNING STORM — Blue storm rush + lightning strike on stop
+ // EFFECT: LIGHTNING STORM — Quick Strobe + Traveling Lightning Bolt
- #define MAX_STORM_PARTICLES 24
+ void effectLightning() {
- 
+   bladeClear();
- struct StormParticle {
+ 
-   float pos, vel, intensity;
+   // ── STATE ─────────────────────────────────────────────────────────────────
-   uint8_t hue;
+   static uint32_t fireStartMs = 0;
-   bool active;
+   static bool     fireLive = false;
- };
+   static bool    … [truncated]

**Facts:**
- Modified 1 files
- identifier: EFFECT
- identifier: LIGHTNING
- identifier: STORM
- identifier: Quick
- identifier: Strobe
- identifier: Traveling
- identifier: Lightning
- identifier: Bolt

**Files:** `EffectLightning.cpp`

---

## Fixed null crash in EFFECT
Source: file-change

- // EFFECT: FIRE — Fireball + Melting Lava on Stop
+ // EFFECT: FIRE — Dual Overlapped Fireball + Idle Traveling Lava
-   // Clear the blade completely
+   bladeClear();
-   bladeClear();
+ 
- 
+   // ── STATE ─────────────────────────────────────────────────────────────────
-   // ── STATE ─────────────────────────────────────────────────────────────────
+   // Fireball
-   // Fireball
+   static uint32_t fireStartMs = 0;
-   static uint32_t fireStartMs = 0;
+   static bool     fireLive = fals… [truncated]

**Facts:**
- Modified 1 files
- identifier: EFFECT
- identifier: FIRE
- identifier: Dual
- identifier: Overlapped
- identifier: Fireball
- identifier: Idle
- identifier: Traveling
- identifier: Lava

**Files:** `EffectFire.cpp`

---

## what-changed in EffectRainbow.cpp
Source: file-change

-     if ((millis() / 30) % 2) {
+     bool fastStrobe = (millis() / 30) % 2;
-       for (int i = HILT_LEDS; i < BLADE_LENGTH; i++) {
+     bool gPetalStrobe = (millis() / 20) % 2;
-         bladeSet(i, CHSV((uint8_t)(millis() * 0.2f + i * 2), 200, 255));
+     uint32_t t = millis();
-       }
+     for (int i = HILT_LEDS; i < BLADE_LENGTH; i++) {
-     } else {
+       if (i >= 19 && i <= 29) {
-       for (int i = HILT_LEDS; i < BLADE_LENGTH; i++) bladeSet(i, CRGB::Black);
+         // gPetal… [truncated]

**Facts:**
- Modified 1 files

**Files:** `EffectRainbow.cpp`

