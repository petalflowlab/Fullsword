# Notes — 2026-03-23
> 7 notes

---

## what-changed in EffectFire.cpp
Source: file-change

-       for (int j = 0; j < 30; j++) {
+       for (int j = 0; j < 40; j++) {
-         CRGB color;
+         
-         if (j < 10) {
+         if (j > 8) {
-           float coreFrac = (float)j / 10.0f;
+           float gapChance = (float)(j - 8) / 32.0f;
-           float twinkle  = random8(200, 255) / 255.0f;
+           if (random8() < (uint8_t)(gapChance * 200)) {
-           uint8_t g = (uint8_t)((1.0f - coreFrac * 0.6f) * 220.0f * twinkle * fbStrobe);
+             continue;
-          … [truncated]

**Facts:**
- Modified 1 files

**Files:** `purplesword/EffectFire.cpp`

---

## what-changed in EffectFire.cpp
Source: file-change

File updated (external): purplesword/EffectFire.cpp

Content summary (359 lines):
#include "Config.h"

// =====================================================================
// EFFECT: FIRE — Melting Lava + Swing Fireball
// (Moved verbatim from the original effectTestIMU)
// =====================================================================

void effectFire() {
  const float dt = 0.016f;
  static float fizzle = 0.0f;
  static float lastSwing = 0.0f;
  static float hiltFlash = 0.0f;

  stat… [truncated]

**Facts:**
- Modified 1 files

**Files:** `purplesword/EffectFire.cpp`

---

## what-changed in purplesword.ino
Source: file-change

File updated (external): purplesword/purplesword.ino

Content summary (617 lines):

// =====================================================================
// Fullsword.ino — Multi-Mode Sword Controller
// =====================================================================
//
// Modes (single-click cycles):
//   0 = Fire        — Melting lava + swing fireball
//   1 = Rainbow     — Paint splashes that mush & mix hilt→tip
//   2 = Lightning   — Blue storm rush + lightning strike on stop
//
// … [truncated]

**Facts:**
- Modified 1 files

**Files:** `purplesword/purplesword.ino`

---

## decision in EffectPOV.cpp — confirmed 3x
Source: file-change

-         for (int i = HILT_LEDS; i < BLADE_LENGTH; i++) {
+         float activeTime = (float)(millis() - strikeStartMs) / 1000.0f;
-             float rf = radFrac(i); // 0 at hilt, 1 at tip
+         // Chaser heavily shoots from base to tip (loops rapidly)
- 
+         float chaserPos = fmodf(activeTime * 3.5f, 1.0f); 
-             float baseHeat = constrain((swingMag - 40.0f) / 200.0f, 0.0f, 1.0f);
+ 
-             float tipWeight = rf * rf; 
+         for (int i = HILT_LEDS; i < BLADE_LEN… [truncated]

**Facts:**
- Modified 1 files

**Files:** `EffectPOV.cpp`

---

## decision in EffectPOV.cpp
Source: file-change

- 
+     static uint32_t strikeStartMs = 0;
-     // Track state transitions for per-swing effects
+ 
-     if (povActive && !wasPovActive) {
+     // Track state transitions for per-swing effects
-         currentRainbowHue += random8(40, 90);  // Shift hue for requested single-color rainbow slash
+     if (povActive && !wasPovActive) {
-     } else if (!povActive && wasPovActive) {
+         currentRainbowHue += random8(40, 90);  // Shift hue for requested single-color rainbow slash
-         … [truncated]

**Facts:**
- Modified 1 files

**Files:** `EffectPOV.cpp`

---

## Fixed null crash in Track
Source: file-change

-     if (!povActive) return;
+     static uint32_t lastPovEndMs = 0;
- 
+     static bool     wasPovActive = false;
-     // Intensity: 0 at 55 deg/s, 1.0 at 250 deg/s.  Clamp both ends.
+     static uint8_t  currentRainbowHue = 0;
-     float intensity = constrain((povPeak - ENTER_SPEED) / 200.0f, 0.0f, 1.0f);
+ 
-     if (intensity < 0.05f) return;
+     // Track state transitions for per-swing effects
- 
+     if (povActive && !wasPovActive) {
-     // ── Overlay alpha: how hard the POV patt… [truncated]

**Facts:**
- Modified 1 files
- identifier: Track
- identifier: Shift
- identifier: Post
- identifier: Strike
- identifier: Effects
- identifier: Crackle
- identifier: CRGB
- identifier: Intensity

**Files:** `EffectPOV.cpp`

---

## decision in EffectPOV.cpp
Source: file-change

-     // MODE 0 — FIRE POV
+     // MODE 0 — FIRE POV (Slash Trails)
-     // What you see in the air: repeating "flame pillars" — orange-yellow
+     // What you see in the air: a thick, sweeping crescent of fire.
-     // vertical columns separated by dark gaps, like upright sheets of
+     // As you sweep, the physical tip (which covers the most physical 
-     // fire hanging in an arc.  The column width is constant in degrees,
+     // area) burns white-hot, while the base stays dark. The i… [truncated]

**Facts:**
- Modified 1 files

**Files:** `EffectPOV.cpp`

