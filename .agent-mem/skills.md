# Learned Patterns — Fullsword
> 22 patterns extracted from your coding history.

## convention

- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 2x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 3x)
- **Pattern**: Always: Added session cookies authentication — confirmed 4x (seen 2x)
- **Pattern**: Always: Added session cookies authentication — externalizes configuration for environ... — confirmed 3x (seen 2x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 4x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 5x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 6x)
- **Pattern**: Always: Added session cookies authentication — confirmed 3x (seen 3x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 7x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 8x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 9x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 10x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 11x)
- **Pattern**: Always: what-changed in brainsync_auto.md — confirmed 3x (seen 12x)
- **Pattern**: Always: Added session cookies authentication — externalizes configuration for environ... — confirmed 3x (seen 3x)
- **Pattern**: Always: Added session cookies authentication — confirmed 3x (seen 2x)

## gotcha

- **Pattern**: Avoid: ⚠️ GOTCHA: Added session cookies authentication — evolves the database schema to support... (seen 2x)
- **Pattern**: Avoid: gotcha in agent-rules.md (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Added session cookies authentication — externalizes configuration for environ... (seen 3x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Added session cookies authentication — evolves the database schema to support... (seen 3x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Added session cookies authentication — externalizes configuration for environ... (seen 2x)
- **Pattern**: Avoid: ⚠️ GOTCHA: Added session cookies authentication — prevents XSS injection attacks (seen 2x)

